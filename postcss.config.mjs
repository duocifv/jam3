/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {}, // Sử dụng Tailwind CSS
    //autoprefixer: {}, // Sử dụng Autoprefixer để tự động thêm tiền tố CSS
  },
}

export default config
